package com.fit.entity;

import com.fit.base.BaseEntity;

public class MemberAttributeMapstoreKey extends BaseEntity {
    /**  (主健ID) (无默认值) */
    private String memberId;

    /**  (主健ID) (无默认值) */
    private String mapkeyId;
}